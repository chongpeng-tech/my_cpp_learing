#include<bits/stdc++>
using namespace std;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    int n,q,temp,num,tempNums,tempItem;
    cin >> n >> q;
    struct box{
        int nums; //编号
        int item; //物品
    };
    vector<box> Box(n + 1);
    for(int i = 0; i < q; i++){
        cin >> temp;
        if(temp == 1){
            cin >> num >> tempNums >> tempItem;
            Box
        }

    }



    return 0;
}