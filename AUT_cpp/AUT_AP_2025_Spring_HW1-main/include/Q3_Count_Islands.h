#ifndef Q3_COUNT_ISLANDS_H
#define Q3_COUNT_ISLANDS_H

// 引入 STL 库，因为函数原型中使用了 std::vector<std::vector<int>>
#include <vector>

// 写入 count_islands 函数声明（来自 README.md）
int count_islands(const std::vector<std::vector<int>>& grid);

#endif // Q3_COUNT_ISLANDS_H

